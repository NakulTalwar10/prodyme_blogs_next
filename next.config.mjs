/** @type {import('next').NextConfig} */
const nextConfig = {
  output:'export',
    // images: {
    //     domains: ["amansamant23.files.wordpress.com"],
    //     unoptimized:true,
    //   },
};

export default nextConfig;
